using System.Windows;
namespace ColorSchemeAddin
{
    // Required code-behind stub for App.xaml in a Revit addin context.
    // Revit hosts WPF so we do NOT call Application.Run() here.
    public partial class WpfApp : Application { }
}
